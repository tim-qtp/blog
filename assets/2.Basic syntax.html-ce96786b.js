import{_ as n,W as s,X as a,a2 as p}from"./framework-28eb7fba.js";const e={},t=p(`<h2 id="一、变量" tabindex="-1"><a class="header-anchor" href="#一、变量" aria-hidden="true">#</a> 一、变量</h2><p>基础语法是一门语言的根，虽然简单，但学习它却是必要的。</p><p>首先我们要学习的便是变量。</p><p>rust中声明变量与其它大多数语言不太一样，它提供了两种变量的声明方式：不可变变量、可变变量。</p><p>虽然听起来很奇怪，名字明明都是变量了，怎么还能不可变呢？</p><p>当深入学习了解rust之后，大概能理解rust这样做的目的：显示的明确开发者写的每一个代码都是有用的，便于编译器根据真实情况进行编译优化。</p><p>比如一个从来不会进行任何修改的变量，那么实际上它和常量也没什么区别，最终编译生成可执行文件时，可能就会将其直接当作常量进行编译优化了。</p><p>首先是不可变变量，使用关键字<code>let</code>：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> a1<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>它的特点便是不可再变，如果你想要更改它的值，就会直接报错：</p><figure><img src="https://qtp-1324720525.cos.ap-shanghai.myqcloud.com/blog/202503090022515.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p><code>vscode</code>中安装了插件后，当你写出错误的代码时，就会直接给你标红。</p><p>如果你此时编译运行它，编译器同样会非常精确的给你标注出来你错在了哪里：</p><figure><img src="https://qtp-1324720525.cos.ap-shanghai.myqcloud.com/blog/202503090022618.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>上图中的错误信息便是不能给不可变变量赋值两次。</p><p>与不可变变量相对的就是可变变量，使用方法如下：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> <span class="token keyword">mut</span> a1<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span>
    a1<span class="token operator">=</span><span class="token number">20</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>只需要在<code>let</code>关键字后面添加一个<code>mut</code>关键字，就可以声明一个可变的变量了。</p><p>除了上面两个变量外，rust中也有常量，也就是不可更改的，需要用到<code>const</code>关键字：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">const</span> a1<span class="token punctuation">:</span><span class="token keyword">i32</span><span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>可以很明显的看到，常量与不可变变量还是有些区别的：</p><ol><li>常量的名称后面必须标注变量类型，比如这里的<code>:i32</code>，<code>i32</code>为rust中的基本数据类型，后面会提到。</li><li><code>const</code>后面不可以添加<code>mut</code>关键字使它可变。</li></ol><p>所以总的来说，rust中声明变量的方式非常简单，只有三个关键字：<code>let</code>、<code>mut</code>、<code>const</code>。</p><p>除却这个，对于变量你还需要注意一点，rust中的变量支持隐藏，比如下面这段代码：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> a<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span>
    <span class="token keyword">let</span> a<span class="token operator">=</span><span class="token char">&#39;c&#39;</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>乍一看，似乎就是让变量a等于两个不同类型的值：<code>数字</code>、<code>字符</code>。</p><p>之所以可以这样写，是因为rust支持变量隐藏，也就是当你后面再用<code>let</code>关键字使用相同的变量名，那么前面那个就被隐藏了。</p><p>比如这里的<code>a</code>，一旦有了第二句，那么在第二句之后再使用<code>a</code>这个变量，都将等于<code>&#39;c&#39;</code>，而前面那个<code>10</code>也并没有在内存中消失，而是被隐藏起来了，你只能在这两句的中间使用到这个数字：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> a<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;a=={}&quot;</span><span class="token punctuation">,</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">//a==10</span>
    <span class="token keyword">let</span> a<span class="token operator">=</span><span class="token char">&#39;c&#39;</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;a=={}&quot;</span><span class="token punctuation">,</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">//a==c</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这个特性方便了我们这些取名困难户，可以给很多变量取一样的名称，但要注意，下面这样写就是错误的：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> <span class="token keyword">mut</span> a<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span>
    a<span class="token operator">=</span><span class="token char">&#39;c&#39;</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>因为这就不是声明一个新的变量来存值，而是想要给原本的变量重新赋值，由于两者类型不同，所以必定会失败。</p><h2 id="二、数据类型" tabindex="-1"><a class="header-anchor" href="#二、数据类型" aria-hidden="true">#</a> 二、数据类型</h2><p>上面我们提到了，所谓的变量就是为了在电脑内存中申请一块内存给我们使用。</p><p>那内存要申请多大的呢？这就涉及到了变量所要存储的数据类型，根据类型的不同，所申请的内存大小也是不同的。</p><p>rust中的数据类型分为两种：<strong>标量</strong> （scalar）和 <strong>复合</strong>（compound）</p><h3 id="_1-标量" tabindex="-1"><a class="header-anchor" href="#_1-标量" aria-hidden="true">#</a> 1.标量</h3><p>首先我们来看看标量，所谓标量，代表的就是单个值，rust中标量分为四种：<strong>整型</strong>、<strong>浮点型</strong>、<strong>布尔类型</strong>和<strong>字符类型</strong>。</p><p>首先我们来看整型：</p><table><thead><tr><th>长度</th><th>有符号</th><th>无符号</th></tr></thead><tbody><tr><td>8-bit</td><td><code>i8</code></td><td><code>u8</code></td></tr><tr><td>16-bit</td><td><code>i16</code></td><td><code>u16</code></td></tr><tr><td>32-bit</td><td><code>i32</code></td><td><code>u32</code></td></tr><tr><td>64-bit</td><td><code>i64</code></td><td><code>u64</code></td></tr><tr><td>128-bit</td><td><code>i128</code></td><td><code>u128</code></td></tr><tr><td>arch</td><td><code>isize</code></td><td><code>usize</code></td></tr></tbody></table><p>前面的长度一列，就代表这个类型要占多大的内存，<code>8bit</code>为1字节。</p><p>而后面的有符号与无符号列，通俗来说就是可正可负的数，以及只能为正数。</p><p>这里的<code>i</code>代表<code>int</code>（ <code>integer</code>），即整数，后面的数字则代表它要占用的内存大小。</p><p>而<code>u</code>则代表<code>unsigned</code>，即无符号，同属来说就是只能存正数，后面的数字同样是它所要占用的内存大小。</p><p>一般我们最常用的就是<code>i32</code>，即一个变量占<code>32</code>位内存大小，并且既可以存储正数，也可以存储负数。</p><p>至于最后的一行<code>arch</code>，<code>isize</code>与<code>usize</code>，则代表它的大小取决于当前系统架构，如果为<code>x86</code>，则它等同于<code>i32</code>与<code>u32</code>，如果为<code>x64</code>，则它等同于<code>i64</code>与<code>u64</code>。</p><p>正如我们所看见的，即使我们不注明类型，它也能为我们推导出来：</p><figure><img src="https://qtp-1324720525.cos.ap-shanghai.myqcloud.com/blog/202503091040499.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>这是因为<code>10</code>这个数字字面量默认为<code>i32</code>类型，所以编译器就可以自动推导出这个变量的类型。</p><p>而一旦不给它赋值，又不注明它的类型，编译器就不知道这个变量需要多大的内存，所以就会直接报错：</p><p>这时我们就可以手动给它注明类型，只要编译器知道它需要多大内存了，就不会报错：</p><figure><img src="https://qtp-1324720525.cos.ap-shanghai.myqcloud.com/blog/202503091043517.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>说完整形，下面就到浮点型了，只有两种：</p><table><thead><tr><th>长度</th><th>类型</th></tr></thead><tbody><tr><td>32-bit</td><td><code>f32</code></td></tr><tr><td>64-bit</td><td><code>f64</code></td></tr></tbody></table><p>唯一需要注意的是，小数字面量为<code>f64</code>类型，比如：</p><figure><img src="https://qtp-1324720525.cos.ap-shanghai.myqcloud.com/blog/202503091857435.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>如果不手动指定，它就会自动推导出<code>f64</code>，紧跟着的便是布尔型，它只有两个可选值：<code>true</code>与<code>false</code></p><figure><img src="https://qtp-1324720525.cos.ap-shanghai.myqcloud.com/blog/202503091857939.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>如果你赋值除了<code>true</code>与<code>false</code>之外的值，它将会直接报错。</p><blockquote><p>在C/C++中只要不为0就是<code>true</code>，这也是两种语言的区别，相比较来看，rust这样对其加以限定可以更加的安全，但对应的也减少了自由度。</p></blockquote><p>最后，就是字符类型了，它主要用于表示一个字符，使用起来也非常的简单：</p><figure><img src="https://qtp-1324720525.cos.ap-shanghai.myqcloud.com/blog/202503091054666.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>注意：rust中的<code>char</code>类型都固定使用4字节（32bit）表示，并且字符是用<strong>单引号</strong>表示，<strong>双引号</strong>指代的是字符串。</p><h3 id="_2-复合类型" tabindex="-1"><a class="header-anchor" href="#_2-复合类型" aria-hidden="true">#</a> 2.复合类型</h3><p>所谓复合类型，就是将多个上面提到的标量组合成的一种类型。</p><p>而Rust有两个原生的复合类型：<strong>元组</strong>（tuple）和<strong>数组</strong>（array）。</p><p>首先来看元组，它可以将多个不同的数据类型放在一起组成一个新的类型，且一旦声明，它的长度就不可增加或减少。</p><p>使用方法如下：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> tup<span class="token operator">=</span><span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">,</span><span class="token char">&#39;余&#39;</span><span class="token punctuation">,</span><span class="token boolean">true</span><span class="token punctuation">,</span><span class="token number">10.1</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>比如上面这个代码，就直接将上面提到的四种标量放在一起，组成了一个元组。</p><p>注意：元组是以<code>()</code>表示的，里面放入各个元素，且各个元素之间用<code>,</code>分隔。</p><p>由于rust可以自动推导出其类型，所以这里我们无需手动为其标注类型：</p><figure><img src="https://qtp-1324720525.cos.ap-shanghai.myqcloud.com/blog/202503091055747.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>如果想要手动标注，就可以根据这个自动推导的格式进行标注即可。</p><p>我们想要取出里面的值也很简单，使用<code>.</code>加数字即可，就像下面这样：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> tup <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">,</span> <span class="token char">&#39;余&#39;</span><span class="token punctuation">,</span> <span class="token boolean">true</span><span class="token punctuation">,</span> <span class="token number">3.14</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{} {} {} {}&quot;</span><span class="token punctuation">,</span> tup<span class="token number">.0</span><span class="token punctuation">,</span> tup<span class="token number">.1</span><span class="token punctuation">,</span> tup<span class="token number">.2</span><span class="token punctuation">,</span> tup<span class="token number">.3</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>注意下标是从0开始的，所以4个元素就只能为<code>0</code>到<code>3</code>。</p><p>这里用到了<code>println!</code>宏进行打印输出，其第一个参数为格式化字符串，字符串中的<code>{}</code>叫做占位符。</p><blockquote><p>意思就是将后面紧跟的参数，挨个放入到这些大括号的位置，然后进行输出，后面会再对它进行更加细致的讲解。</p></blockquote><p>得到的输出结果就是：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token number">100</span> 余 <span class="token boolean">true</span> <span class="token number">3.14</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果元组里面没有任何参数，则称它为<strong>单元</strong>（unit） 元组，直接写作<code>（）</code>，代表空值。</p><p>除了元组外，还有数组也很常用，它的使用方法与元组还是有点差距的，其中最大的差别就是，数组中的所有元素的类型必须相同，而上面提到的元组各个元素类型可以不同。</p><p>使用方法如下：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> arr<span class="token operator">=</span><span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span><span class="token number">2</span><span class="token punctuation">,</span><span class="token number">3</span><span class="token punctuation">,</span><span class="token number">4</span><span class="token punctuation">,</span><span class="token number">5</span><span class="token punctuation">,</span><span class="token number">6</span><span class="token punctuation">,</span><span class="token number">7</span><span class="token punctuation">,</span><span class="token number">8</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{} {} {} {}&quot;</span><span class="token punctuation">,</span>arr<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">,</span>arr<span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">,</span>arr<span class="token punctuation">[</span><span class="token number">2</span><span class="token punctuation">]</span><span class="token punctuation">,</span>arr<span class="token punctuation">[</span><span class="token number">3</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>注意它的大小同样是不可变的，想要可变长的，后面会使用<code>Vec</code>，这里先略过。</p><p>它的声明使用<code>[]</code>，访问其中的元素同样是用的<code>[]</code>中间添加下标的方式进行访问，而且也是从0开始的。</p><p>如果你想要手动更改元素类型，可以像这里自动推导一样，手写自己想要的类型即可：</p><figure><img src="https://qtp-1324720525.cos.ap-shanghai.myqcloud.com/blog/202503091101666.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>比如改为 <code>[i64;8]</code>，前面的为数据类型，后面的为元素个数。</p><p>除此之外，如果你想要赋值一个数组同样的值，那就可以像下面这样写:</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">let</span> arr<span class="token operator">=</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">;</span><span class="token number">5</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这代表我要申请一个数组有5个元素，每个元素都等于<code>0</code>。</p><h2 id="三、函数" tabindex="-1"><a class="header-anchor" href="#三、函数" aria-hidden="true">#</a> 三、函数</h2><p>下面要介绍的便是rust中的函数了，其实在介绍函数前，我们就已经使用过函数了，最常见的<code>main</code>入口函数，就是一个函数。</p><p>函数的目的就是可以让我们重复的使用一些代码，比如每声明一个变量，我就要打印它三次，没有函数时，就得这样写：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> a<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span> 
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token keyword">let</span> a<span class="token operator">=</span><span class="token number">20</span><span class="token punctuation">;</span>  <span class="token comment">//用到变量隐藏的特性</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>明显打印的这个步骤有些重复了，所以我们就可以将其封装为一个函数，比如下面这样：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> a<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span> 
    <span class="token function">printNum</span><span class="token punctuation">(</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token keyword">let</span> a<span class="token operator">=</span><span class="token number">20</span><span class="token punctuation">;</span>  <span class="token comment">//用到变量隐藏的特性</span>
    <span class="token function">printNum</span><span class="token punctuation">(</span>a<span class="token punctuation">)</span>
<span class="token punctuation">}</span>

<span class="token keyword">fn</span> <span class="token function-definition function">printNum</span><span class="token punctuation">(</span>i<span class="token punctuation">:</span><span class="token keyword">i32</span><span class="token punctuation">)</span><span class="token punctuation">{</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>i<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>i<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>i<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>通过将打印的这个步骤封装成为一个函数，那我们就可以方便的调用它来执行任务了。</p><p>如果学过C/C++可能会觉得这样写有点奇怪，因为调用函数的地方居然在函数定义的前面，但在rust中不存在前后关系，它们都同处于一个作用域。</p><p>一个最简单的函数长下面这样：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">simple</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span>
    
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>为了能让这个函数干一些事情，我们就需要在函数体，也就是<code>{}</code>中写一些代码：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">simple</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;代码&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;code&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token comment">//.....</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这时，它就可以执行一些任务了。</p><p>但只是这样还不够，在没有任何输入的情况下，这个函数能做的事情基本就写死了：无论任何地方调用它，其结果都是一样的（调用随机数除外）。</p><p>所以我们就需要函数参数，也就是从函数外部传入的变量，可以让函数内部来使用，参数写在<code>()</code>中。</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">simple</span><span class="token punctuation">(</span>i<span class="token punctuation">:</span><span class="token keyword">i32</span><span class="token punctuation">,</span>c<span class="token punctuation">:</span><span class="token keyword">char</span><span class="token punctuation">,</span>f<span class="token punctuation">:</span><span class="token keyword">f64</span><span class="token punctuation">,</span>b<span class="token punctuation">:</span><span class="token keyword">bool</span><span class="token punctuation">)</span><span class="token punctuation">{</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{} {} {} {}&quot;</span><span class="token punctuation">,</span>i<span class="token punctuation">,</span>c<span class="token punctuation">,</span>f<span class="token punctuation">,</span>b<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>函数的参数可以有任意多个，唯一需要注意的是，各个参数都必须在其后注明类型，并且多个参数之间要用<code>,</code>分隔。</p><p>如果不注明，编译器也无法推断它是什么类型，从而不知道它要占用多大的内存，由此编译失败。</p><p>调用它也很简单，写上函数名称、并在<code>()</code>中顺序填写对应的参数就行了</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token function">simple</span><span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">,</span> <span class="token char">&#39;c&#39;</span><span class="token punctuation">,</span> <span class="token number">3.1415</span><span class="token punctuation">,</span> <span class="token boolean">true</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>但仅仅只是这样仍旧不够，比如我想用函数计算两个数字之和，函数计算完成后，得把结果送回来呀！</p><p>这就用到了函数的返回值：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">sum</span><span class="token punctuation">(</span>a<span class="token punctuation">:</span><span class="token keyword">i32</span><span class="token punctuation">,</span>b<span class="token punctuation">:</span><span class="token keyword">i32</span><span class="token punctuation">)</span> <span class="token punctuation">-&gt;</span> <span class="token keyword">i32</span><span class="token punctuation">{</span>
    <span class="token keyword">return</span> a<span class="token operator">+</span>b<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>只需要在<code>()</code>与<code>{}</code>之间，用 <code>-&gt;</code>指明这个返回值的类型即可，因为这里要返回数字和，所以用的<code>i32</code>类型。</p><p>然后在函数内部，通过<code>return</code>关键字，返回运算结果就行了。</p><p>当你调用这个函数时，就可以用一个变量来接收它的返回值并输出：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> s<span class="token operator">=</span><span class="token function">sum</span><span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">,</span><span class="token number">200</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;a+b={}&quot;</span><span class="token punctuation">,</span>s<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这就是rust中最简单的函数用法，但事实上，rust中的函数还有很多高级特性，这个就留在后面再讲了。</p><h2 id="四、注释" tabindex="-1"><a class="header-anchor" href="#四、注释" aria-hidden="true">#</a> 四、注释</h2><p>但rust中的注释比其它大部分语言的注释都还要更高级一点，因为它除了正常的注释外，还支持文档注释。</p><p>所谓文档注释，就是你在代码中写的注释，<code>cargo</code>可以一键帮你生成文档。</p><p>首先来看普通注释：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> a<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span> <span class="token comment">//声明一个类型为i32的变量</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>除了普通的注释，我们还可以写文档注释，用于生成对应的文档。</p><p>比如上面介绍函数时，就写了一个求和函数<code>sum</code>，我们就可以为这个函数写文档注释：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> s<span class="token operator">=</span><span class="token function">sum</span><span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">,</span><span class="token number">200</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;a+b={}&quot;</span><span class="token punctuation">,</span>s<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token comment">/// 这是一个求和函数</span>
<span class="token comment">/// # 例子</span>
<span class="token comment">/// \`\`\`</span>
<span class="token comment">/// let s=sum(100,200)</span>
<span class="token comment">/// \`\`\`</span>
<span class="token keyword">fn</span> <span class="token function-definition function">sum</span><span class="token punctuation">(</span>a<span class="token punctuation">:</span><span class="token keyword">i32</span><span class="token punctuation">,</span>b<span class="token punctuation">:</span><span class="token keyword">i32</span><span class="token punctuation">)</span> <span class="token punctuation">-&gt;</span> <span class="token keyword">i32</span><span class="token punctuation">{</span>
    <span class="token keyword">return</span> a<span class="token operator">+</span>b<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>注意文档注释是用的<code>///</code>，直接写在函数的上方即可，并且这里的注释是<code>markdown</code>格式，比如这里的<code>#</code>代表这是个一级标题，而代码前后的三个上漂，就代表中间写的是代码。</p><p>然后我们回到终端运行命令：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cargo</span> doc <span class="token parameter variable">--open</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>该文档注释就会被编译，并且自动从电脑默认浏览器打开一个帮助文档：</p><figure><img src="https://qtp-1324720525.cos.ap-shanghai.myqcloud.com/blog/202503091110215.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>这里就可以看到我们的函数<code>sum</code>的注释了，然后点进去，你就能看到完整的注释：</p><figure><img src="https://qtp-1324720525.cos.ap-shanghai.myqcloud.com/blog/202503091110901.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>上面这种方式是给指定的函数写注释并生成文档的方式。</p><p>但有时我们还需要为当前整个库写注释，比如算法库，里面有求和函数、求最大值函数等等。</p><p>为了给这个整体库写注释，我们需要用到<code>\\\\!</code>，还是上面的那个例子，为当前程序写个注释：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token comment">//! # 我的文档注释</span>
<span class="token comment">//!</span>
<span class="token comment">//! 这是一个测试文档的例子程序</span>


<span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> s<span class="token operator">=</span><span class="token function">sum</span><span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">,</span><span class="token number">200</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;a+b={}&quot;</span><span class="token punctuation">,</span>s<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token comment">/// 这是一个求和函数</span>
<span class="token comment">/// # 例子</span>
<span class="token comment">/// \`\`\`</span>
<span class="token comment">/// let s=sum(100,200)</span>
<span class="token comment">/// \`\`\`</span>
<span class="token keyword">fn</span> <span class="token function-definition function">sum</span><span class="token punctuation">(</span>a<span class="token punctuation">:</span><span class="token keyword">i32</span><span class="token punctuation">,</span>b<span class="token punctuation">:</span><span class="token keyword">i32</span><span class="token punctuation">)</span> <span class="token punctuation">-&gt;</span> <span class="token keyword">i32</span><span class="token punctuation">{</span>
    <span class="token keyword">return</span> a<span class="token operator">+</span>b<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>然后运行命令 <code>cargo doc --open</code></p><figure><img src="https://qtp-1324720525.cos.ap-shanghai.myqcloud.com/blog/202503091112267.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>就可以看到当前整个库都有注释了。</p><p>注意我这里虽然都说的是库，但实际上<code>rust</code>给库定义了一个新名词：<code>crate</code></p><p>翻译过来就是箱子的意思，即装各种东西的箱子。</p><h2 id="五、控制流" tabindex="-1"><a class="header-anchor" href="#五、控制流" aria-hidden="true">#</a> 五、控制流</h2><p>控制流，说白了就是控制程序运行流程的，比如通过前面的代码，你应该已经建立起了这样的一个认知：</p><p>程序是从<code>main</code>函数的函数体开始，从上到下，遇到一句代码就执行一句代码，遇到一个函数，就跳到函数所在的位置执行完再返回原处，然后继续向下执行。</p><p>但很多时候我们并不希望这样，比如我们要判断用户输入的一个数字，如果大于0，我们就执行大于0的语句，小于0，我们就要执行小于0的语句。</p><p>这就不能直接顺序执行下来了。</p><p>还有前面曾经用过打印语句，只打印了三次还好，但如果要打三百次、三千次呢？我们同样不能手写三千条相同的语句吧！</p><p>所以这时候我们就需要改变程序原本顺序执行的流程，首先就是判断语句，例子如下：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> a<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> a<span class="token operator">&gt;</span><span class="token number">0</span> <span class="token punctuation">{</span>
        <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;a&gt;0&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span><span class="token keyword">else</span><span class="token punctuation">{</span>
        <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;a&lt;=0&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>判断语句用到的就是<code>if</code>关键字，后面紧跟要判断的布尔类型，如果为<code>true</code>，则执行紧跟其后<code>{}</code>中的内容；如果为<code>false</code>，则执行关键字<code>else</code>后面<code>{}</code>中的内容。</p><p>这样就实现了在不同情况下执行不同语句的目的。</p><p>当然，<code>else</code>以及紧跟其后的语句是可以省略的，这样的效果就是只要<code>if</code>后面的表达式不为<code>true</code>，那就什么都不执行。</p><p>或者我们也可以增加判断分支数量：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
   <span class="token keyword">let</span> a<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span>
   <span class="token keyword">if</span> a<span class="token operator">&gt;</span><span class="token number">0</span> <span class="token punctuation">{</span>
        <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;a&gt;0&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span><span class="token keyword">else</span> <span class="token keyword">if</span> a<span class="token operator">==</span><span class="token number">0</span> <span class="token punctuation">{</span>
        <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;a==0&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span><span class="token keyword">else</span><span class="token punctuation">{</span>
        <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;a&lt;0&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>增加的方式就是在<code>if</code>语句的后面、<code>else</code>语句的前面，增加<code>else if</code>语句，并紧跟需要判断的语句即可。</p><p>基本的比较运算符我就不再多讲了，比如这里的 <code>a&gt;0</code> <code>a==0</code>就是两个比较运算符，分别代表大于、等于，如果确实满足，那么这个运算符就会返回<code>true</code>，否则就会返回<code>false</code>。</p><p>注意：在代码中等于是两个<code>=</code>，一个<code>=</code>叫做赋值。</p><p>总的来说判断语句还是很简单的，下面我们再来介绍一下<code>rust</code>中的循环语句，也就是如何能用简短的几句代码，重复执行某些代码无数次。</p><p><code>rust</code>中的循环语句共有三个：<code>for</code>、<code>while</code>以及<code>loop</code>。</p><p>为了简便，我们从<code>loop</code>开始说起，它的使用方法非常简单:</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> a<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span>
    <span class="token keyword">loop</span> <span class="token punctuation">{</span>
        <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>只需要一个<code>loop</code>关键字后面跟着<code>{}</code>，而<code>{}</code>中就是我们想要循环的语句。</p><p>注意：这个语句是无限循环，这就是说，一旦你运行这个代码，程序就停不下来，会一直打印数字<code>10</code>。</p><p>此时可以在终端按<code>Ctrl+C</code>快捷键强制终止运行。</p><p>比如在第一章中我提到过，当你点击运行其编译的可执行文件，只会闪过一道黑影，原因就是程序很快执行结束、退出导致的。</p><p>而当你在代码的最后一行写入上面这样一个死循环，就意味着程序永远不会退出，此时当你编译它、然后点击运行，就能看到黑窗口了。</p><p>如果想要让它停下来，我们还需要用到另一个关键字<code>break</code>，即跳出当前循环的意思：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> <span class="token keyword">mut</span> a<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span>
    <span class="token keyword">loop</span> <span class="token punctuation">{</span>
        <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span>
        a<span class="token operator">=</span>a<span class="token operator">+</span><span class="token number">1</span><span class="token punctuation">;</span>
        <span class="token keyword">if</span> a<span class="token operator">==</span><span class="token number">20</span><span class="token punctuation">{</span>
            <span class="token keyword">break</span><span class="token punctuation">;</span>
        <span class="token punctuation">}</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>上面这段代码的意思就是，每打印一次，我就让a的数字加一，如果等于20了，我就调用<code>break</code>，跳出当前循环，然后程序结束。</p><p>但这样还是有点麻烦，每次都需要我自己判断是否跳出，这时候就有了<code>while</code>语句：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> <span class="token keyword">mut</span> a<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span>
    <span class="token keyword">while</span> <span class="token macro property">a!</span><span class="token operator">=</span><span class="token number">20</span> <span class="token punctuation">{</span>
        <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span>
        a<span class="token operator">=</span>a<span class="token operator">+</span><span class="token number">1</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>while</code>语句与<code>loop</code>的区别就在于，在<code>while</code>与<code>{}</code>之间，可以填写一个布尔表达式。</p><p>我这里填写的是 <code>a!=20</code>，意思就是，只要<code>a</code>不等于<code>20</code>，就会一直循环后面的语句。</p><p>但由于我在循环体内部每次都加了一个<code>1</code>，所以当<code>a==20</code>的时候，<code>while</code>后面的布尔表达式为<code>false</code>，然后就自动跳出了循环，不需要我们自己调用<code>break</code>。</p><p>当然，如果你想要调<code>break</code>，仍然是可以调用的。</p><p>但如果每次遍历一系列的数字都这样写，我们可能仍然还是会觉得麻烦，这时候就有了<code>for</code>语句：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">for</span> i <span class="token keyword">in</span> <span class="token number">10</span><span class="token punctuation">..</span><span class="token number">20</span><span class="token punctuation">{</span>
        <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>i<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>for</code>循环中有两个关键字，<code>for</code>与<code>in</code>，其中<code>in</code>后面的就是要遍历的对象，通过语法 <code>10..20</code>就可以自动生成一个<code>10</code>到<code>20</code>的范围对象，并将该对象中的值依次赋值给<code>for</code>后面的变量<code>i</code>。</p><p>然后我们就可以在循环体中使用这个变量名<code>i</code>，一旦遍历完成，就会自动退出。</p><blockquote><p>注意范围运算符<code>..</code>生成的范围对象是左闭右开的，具体来说，<code>10..20</code>中<code>i</code>只会等于<code>10</code>到<code>19</code>，也就是<code>[10,20)</code>。</p></blockquote><p>前期知道它是如何用的就行了，后面有机会再对它介绍。</p><p>当你使用一个变量等于它时，也会自动为你推导出它的类型：</p><figure><img src="https://qtp-1324720525.cos.ap-shanghai.myqcloud.com/blog/202503091121316.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>除了基本的遍历数字外，它还可以遍历数组，但不能遍历元组：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> arr<span class="token operator">=</span><span class="token punctuation">[</span><span class="token number">10</span><span class="token punctuation">,</span><span class="token number">20</span><span class="token punctuation">,</span><span class="token number">30</span><span class="token punctuation">,</span><span class="token number">40</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token keyword">let</span> t<span class="token operator">=</span><span class="token punctuation">(</span><span class="token number">10</span><span class="token punctuation">,</span><span class="token char">&#39;c&#39;</span><span class="token punctuation">,</span><span class="token boolean">false</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token comment">//正确，可以遍历数组，因为每个数组的元素类型都相同</span>
    <span class="token keyword">for</span> i <span class="token keyword">in</span> arr<span class="token punctuation">{</span>
        <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>i<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
    <span class="token comment">//错误，由于元组元素类型可能不相同，所以不能这样遍历</span>
    <span class="token comment">// for i in t{</span>
    <span class="token comment">//     println!(&quot;{}&quot;,i);</span>
    <span class="token comment">// }</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>当然，只要是循环，就支持使用<code>break</code>关键字，<code>for</code>循环也不例外，这里不再过多赘述。</p><p>事实上，除却<code>break</code>关键字外，还有一个<code>continue</code>关键字，它不像<code>break</code>会直接跳出当前循环，它仅仅只是跳过当下这一次的循环。</p><p>比如，我想要输出<code>0</code>到<code>10</code>之间的奇数：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">for</span> i <span class="token keyword">in</span> <span class="token number">0</span><span class="token punctuation">..</span><span class="token number">10</span><span class="token punctuation">{</span>
        <span class="token keyword">if</span> i<span class="token operator">%</span><span class="token number">2</span><span class="token operator">==</span><span class="token number">0</span> <span class="token punctuation">{</span> <span class="token comment">//如果是偶数</span>
            <span class="token keyword">continue</span><span class="token punctuation">;</span>  <span class="token comment">//那就跳过当前循环，不执行后面的语句</span>
        <span class="token punctuation">}</span>
        <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">&quot;{}&quot;</span><span class="token punctuation">,</span>i<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>%</code>是取余符号，如果一个数字除于<code>2</code>余<code>0</code>，那就是偶数，就调用<code>continue</code>跳过当前循环，不执行后面的打印函数。</p><p>否则就是奇数，不执行<code>continue</code>，也就是不跳过当前循环，执行后面的打印语句。</p><p>这里介绍的是最基础的语法，rust中还有很多更加高级的用法，就留到后面章节再介绍了。</p><h2 id="六、简单总结" tabindex="-1"><a class="header-anchor" href="#六、简单总结" aria-hidden="true">#</a> 六、简单总结</h2><p><code>rust</code>中分为<code>let</code>、<code>mut</code>、<code>const</code>三个关键字用于声明变量与常量。</p><p>如果为<code>let</code>，代表变量不可变，如果用<code>let mut</code>，则代表变量可变，如果为<code>const</code>，则代表这是一个常量，必须标注数据类型，并且其后不可跟<code>mut</code>使之可变。</p><p>然后是数据类型，分为两类：<strong>标量</strong>与<strong>复合类型</strong>。</p><p>其中标量有四个类型：<strong>整型</strong>、<strong>浮点型</strong>、<strong>布尔类型</strong>和<strong>字符类型</strong>。</p><p>而复合类型只有两个：<strong>元组</strong>、<strong>数组</strong>。</p><p>其中如果直接给变量赋值的话，可以省略类型注解，因为编译器可以直接推导出来。</p><p>但如果你想要手动指定、或者不想赋值，那就可以自己手写类型注解，方法就是在变量名后面添加一个<code>:</code>然后写上类型就行了。</p><p>比如：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">let</span> a<span class="token punctuation">:</span><span class="token keyword">i32</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>注意元组与数组最大的区别有两点：</p><ol><li>元组可以存放不同类型的元素，而数组只能存放同种类型的元素</li><li>元素访问通过<code>.数字下标</code>的方式，而数组则使用的<code>[数字下标]</code></li></ol><p>相同点就是两者的长度一旦声明了，就不可以再更改了</p><p>然后是函数，用关键字<code>fn</code>声明，既有参数，又有返回值的函数长下面这样：</p><div class="language-rust line-numbers-mode" data-ext="rs"><pre class="language-rust"><code><span class="token keyword">fn</span> <span class="token function-definition function">name</span><span class="token punctuation">(</span>a<span class="token punctuation">:</span><span class="token keyword">i32</span><span class="token punctuation">)</span> <span class="token punctuation">-&gt;</span> <span class="token keyword">i32</span> <span class="token punctuation">{</span>
	<span class="token keyword">return</span> a<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>然后是注释，rust中注释分为三种：</p><ol><li><code>\\\\</code>或<code>\\**\\</code>：普通注释，一般用于注释特定的几行代码</li><li><code>\\\\\\</code>：文档注释，一般用于注释函数，使用的<code>markdown</code>格式，可以直接在生成的文档中查看。</li><li><code>\\\\!</code>：文档注释，一般用于注释当前这个<code>Crate</code>（箱子，或者理解为库），同样可以在生成的文档中查看</li></ol><p>生成文档的命令为：<code>cargo doc</code>。</p><p>生成并在浏览器中打开文档的命令为：<code>cargo doc --open</code>。</p><p>最后还有控制流，分为判断语句、循环语句两种</p><ul><li>判断语句：可通过<code>if</code>、<code>else if</code> 、<code>else</code>建立分支语句，根据不同情况来执行不同的语句。</li><li>循环语句：有<code>loop</code>、<code>while</code>、<code>for</code>，本质都是不断循环一段语句，但其能力是从左到右逐渐增强的。</li></ul>`,217),o=[t];function c(i,l){return s(),a("div",null,o)}const d=n(e,[["render",c],["__file","2.Basic syntax.html.vue"]]);export{d as default};
